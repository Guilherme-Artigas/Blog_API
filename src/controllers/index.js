const loginController = require('./login.controller');
const userController = require('./user.controller');
const categoriesController = require('./categorie.controller');

module.exports = {
  loginController,
  userController,
  categoriesController,
};
